1. NSURL has functions for parsing search:// URLs
2. Allow a SPSearchResult category for search:// URLs with strings as identifiers.

1. Load bundles from /Library/SearchLoader/SearchBundles

1. Do not rely on 15 as start for domains.

1. Remove AppIndexer errors (destroying index, etc.)

1. Remove creation of record stores.
